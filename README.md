# project

limit在分页查询中用法
limit 本身是对数据偏移量起到一定作用
limit在分页的过程中起到了很好的作用，在显示页码上很好的起到了数据的偏移作用，文字的描述往往不能直击头脑，视觉直击更加突出。
例如 limit (3,2),则表示数据从3开始向后偏移2个数据，在分页中一般表示，limit（当前页面的索引值，页面大小），limit(3,2)则表示页面的索引起始是3，页面大小是2，页面显示数据为从索引为3开始向后查询2条数据后输出显示
